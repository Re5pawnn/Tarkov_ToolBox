<UserControl x:Class="TarkovMapLocator.Modules.Utilities.SeasonDocuments.SeasonDocumentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:TarkovMapLocator.Modules.Utilities.Controls"
             Background="#101514" FontFamily="Microsoft YaHei UI">
  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="88" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
    <Grid Margin="24,20,24,16">
      <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="16" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
      <Button Style="{StaticResource PageToolbarButton}" Width="72" Height="34" VerticalAlignment="Bottom" Click="Back_Click">← 返回</Button>
      <StackPanel Grid.Column="2" VerticalAlignment="Bottom"><TextBlock Text="UTILITY HUB" Style="{StaticResource SectionLabel}" /><TextBlock Text="赛季文档刷新位置" Style="{StaticResource PageTitle}" /></StackPanel>
    </Grid>
    <WrapPanel Grid.Row="1" Margin="24,0,24,0">
      <ComboBox x:Name="DocumentSelector" Width="230" Height="36" Margin="0,0,10,8" Style="{StaticResource FormComboBox}" DisplayMemberPath="LocalizedName" SelectionChanged="DocumentSelector_SelectionChanged" />
      <ComboBox x:Name="MapSelector" Width="180" Height="36" Margin="0,0,10,8" Style="{StaticResource FormComboBox}" SelectionChanged="MapSelector_SelectionChanged" />
      <Border Width="280" Height="36" Margin="0,0,0,8" BorderBrush="{StaticResource LineBrightBrush}" BorderThickness="1" Background="#111715" CornerRadius="7">
        <Grid Margin="10,0"><Grid.ColumnDefinitions><ColumnDefinition Width="19" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><TextBlock Text="⌕" Foreground="{StaticResource TextFaintBrush}" FontSize="15" VerticalAlignment="Center" /><TextBox Grid.Column="1" x:Name="SearchBox" TextChanged="SearchBox_TextChanged" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextBrush}" CaretBrush="{StaticResource AmberBrush}" FontSize="12" Padding="0" VerticalContentAlignment="Center" /></Grid>
      </Border>
    </WrapPanel>
    <TextBlock Grid.Row="2" x:Name="StatusText" Foreground="{StaticResource TextFaintBrush}" FontSize="11" Margin="24,10,24,9" />
    <Border Grid.Row="3" Margin="24,0,24,24" Background="#111715" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="10">
      <Grid>
        <ListBox x:Name="ResultsList" Background="Transparent" BorderThickness="0" Padding="12" ItemContainerStyle="{StaticResource CardListItem}" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" VirtualizingPanel.ScrollUnit="Pixel">
          <ListBox.ItemsPanel><ItemsPanelTemplate><VirtualizingStackPanel /></ItemsPanelTemplate></ListBox.ItemsPanel>
          <ListBox.ItemTemplate>
            <DataTemplate>
              <Border Background="#151C19" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,9">
                <WrapPanel>
                  <controls:ModuleAsyncImage ImageId="{Binding ImageCacheId}" ImageUrl="{Binding ImageUrl}" Width="460" Height="259" Margin="0,0,18,0" PreviewClicked="Preview_Click" />
                  <StackPanel Width="280" VerticalAlignment="Center">
                    <Border HorizontalAlignment="Left" Background="#242B27" CornerRadius="8" Padding="8,3"><TextBlock Text="{Binding MapLocalizedName}" Foreground="{StaticResource CyanBrush}" FontSize="10" FontWeight="SemiBold" /></Border>
                    <TextBlock Text="{Binding Title}" Foreground="{StaticResource TextBrush}" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,10,0,0" />
                    <Border Margin="0,12,0,0" Padding="10,8" Background="#111715" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="6">
                      <Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Visible" /><Style.Triggers><DataTrigger Binding="{Binding HasKeys}" Value="False"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers></Style></Border.Style>
                      <StackPanel><TextBlock Text="所需钥匙" Foreground="{StaticResource AmberBrush}" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,5" /><ItemsControl ItemsSource="{Binding Keys}"><ItemsControl.ItemTemplate><DataTemplate><StackPanel Margin="0,2"><TextBlock Text="{Binding LocalizedName}" Foreground="{StaticResource TextBrush}" FontSize="11" TextWrapping="Wrap" /><TextBlock Text="{Binding Name}" Foreground="{StaticResource TextFaintBrush}" FontSize="9" Margin="0,2,0,0" TextWrapping="Wrap" /></StackPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel>
                    </Border>
                  </StackPanel>
                </WrapPanel>
              </Border>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
        <TextBlock x:Name="EmptyText" Text="没有符合条件的刷新点。" Visibility="Collapsed" Foreground="{StaticResource TextFaintBrush}" FontSize="12" Margin="16" />
      </Grid>
    </Border>
  </Grid>
</UserControl>
