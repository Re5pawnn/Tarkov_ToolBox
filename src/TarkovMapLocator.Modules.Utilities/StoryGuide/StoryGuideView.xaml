<UserControl x:Class="TarkovMapLocator.Modules.Utilities.StoryGuide.StoryGuideView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:TarkovMapLocator.Modules.Utilities.Controls"
             Background="#101514" FontFamily="Microsoft YaHei UI"
             SizeChanged="View_SizeChanged">
  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="88" /><RowDefinition Height="*" /></Grid.RowDefinitions>
    <Grid Margin="24,20,24,16">
      <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="16" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
      <Button Style="{StaticResource PageToolbarButton}" Width="72" Height="34" VerticalAlignment="Bottom" Click="Back_Click">← 返回</Button>
      <StackPanel Grid.Column="2" VerticalAlignment="Bottom"><TextBlock Text="UTILITY HUB" Style="{StaticResource SectionLabel}" /><TextBlock Text="剧情攻略" Style="{StaticResource PageTitle}" /></StackPanel>
    </Grid>
    <Grid Grid.Row="1" Margin="24,0,24,24">
      <Grid.ColumnDefinitions><ColumnDefinition x:Name="ChapterColumn" Width="278" /><ColumnDefinition x:Name="ColumnGap" Width="12" /><ColumnDefinition x:Name="DetailColumn" Width="*" /></Grid.ColumnDefinitions>
      <Grid.RowDefinitions><RowDefinition x:Name="ChapterRow" Height="*" /><RowDefinition x:Name="RowGap" Height="0" /><RowDefinition x:Name="DetailRow" Height="0" /></Grid.RowDefinitions>
      <Border x:Name="ChapterPane" Grid.RowSpan="3" Background="#111715" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="10">
        <ListBox x:Name="ChapterList" Background="Transparent" BorderThickness="0" Padding="9" ItemContainerStyle="{StaticResource CardListItem}" SelectionChanged="ChapterList_SelectionChanged" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
          <ListBox.ItemTemplate><DataTemplate><Border Padding="12,10" Margin="0,0,0,7" Background="#151C19" BorderBrush="{StaticResource LineBrightBrush}" BorderThickness="1" CornerRadius="7"><StackPanel><TextBlock Text="{Binding Name}" Foreground="{StaticResource TextBrush}" FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap" /><TextBlock Foreground="{StaticResource TextFaintBrush}" FontSize="10" Margin="0,4,0,0" TextWrapping="Wrap"><Run Text="{Binding EnglishName}" /><Run Text=" · " /><Run Text="{Binding Category}" /></TextBlock></StackPanel></Border></DataTemplate></ListBox.ItemTemplate>
        </ListBox>
      </Border>
      <Grid x:Name="DetailPane" Grid.Column="2" Grid.RowSpan="3">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Border Padding="16" Background="#111715" BorderBrush="{StaticResource LineBrightBrush}" BorderThickness="1" CornerRadius="10">
          <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel>
              <TextBlock x:Name="TitleText" Foreground="{StaticResource TextBrush}" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap" />
              <TextBlock x:Name="EnglishNameText" Foreground="{StaticResource AmberBrush}" FontSize="11" FontWeight="SemiBold" Margin="0,4,0,0" />
              <TextBlock x:Name="SummaryText" Foreground="{StaticResource TextDimBrush}" FontSize="12" LineHeight="21" TextWrapping="Wrap" Margin="0,9,16,0" />
            </StackPanel>
            <WrapPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
              <ComboBox x:Name="ModeSelector" Width="88" Height="32" Margin="0,0,7,0" Style="{StaticResource DarkComboBox}" Padding="9,0,25,0" SelectedValuePath="Tag" SelectionChanged="ModeSelector_SelectionChanged" ToolTip="选择剧情攻略的游戏模式" AutomationProperties.Name="剧情攻略模式">
                <ComboBoxItem Tag="pvp" Content="PVP" />
                <ComboBoxItem Tag="pve" Content="PVE" />
              </ComboBox>
              <ComboBox x:Name="VariantSelector" Width="230" Height="32" Margin="0,0,7,0" Style="{StaticResource DarkComboBox}" Padding="9,0,25,0" DisplayMemberPath="Name" Visibility="Collapsed" SelectionChanged="VariantSelector_SelectionChanged" ToolTip="选择门票剧情路线" />
              <Button Style="{StaticResource PageToolbarButton}" Height="32" Padding="11,0" Click="Refresh_Click">刷新</Button>
              <Button Style="{StaticResource PageToolbarButton}" Height="32" Padding="11,0" Margin="7,0,0,0" Click="Source_Click">打开来源</Button>
            </WrapPanel>
          </Grid>
        </Border>
        <Border x:Name="RequirementsCard" Grid.Row="1" Margin="0,10,0,0" Padding="14" Background="#151C19" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="8">
          <StackPanel><TextBlock Text="解锁条件" Foreground="{StaticResource AmberBrush}" FontSize="12" FontWeight="SemiBold" /><ItemsControl x:Name="RequirementsList" Margin="0,6,0,0"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding StringFormat=• {0}}" Foreground="{StaticResource TextDimBrush}" FontSize="11" LineHeight="20" TextWrapping="Wrap" Margin="0,2,0,0" /></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel>
        </Border>
        <Grid Grid.Row="2" Margin="0,10,0,0">
          <ListBox x:Name="StepsList" Background="Transparent" BorderThickness="0" Padding="0" ItemContainerStyle="{StaticResource CardListItem}" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" VirtualizingPanel.ScrollUnit="Pixel">
            <ListBox.ItemsPanel><ItemsPanelTemplate><VirtualizingStackPanel /></ItemsPanelTemplate></ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
              <DataTemplate>
                <Border Margin="0,0,0,9" Padding="14" Background="#111715" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="8">
                  <StackPanel>
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="34" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><Border Width="26" Height="26" Background="#2A251A" BorderBrush="{StaticResource AmberBrush}" BorderThickness="1" CornerRadius="5" VerticalAlignment="Top"><TextBlock Text="{Binding Number}" Foreground="{StaticResource AmberBrush}" FontFamily="Cascadia Mono" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" /></Border><TextBlock Grid.Column="1" Text="{Binding Title}" Foreground="{StaticResource TextBrush}" FontSize="13" FontWeight="SemiBold" LineHeight="21" TextWrapping="Wrap" VerticalAlignment="Center" /></Grid>
                    <ItemsControl ItemsSource="{Binding Notes}" Margin="34,7,0,0"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding StringFormat=• {0}}" Foreground="{StaticResource TextDimBrush}" FontSize="11" LineHeight="20" TextWrapping="Wrap" Margin="0,2,0,0" /></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <ItemsControl ItemsSource="{Binding Images}" Margin="34,5,0,0"><ItemsControl.ItemTemplate><DataTemplate><StackPanel Margin="0,7,0,0"><TextBlock Text="{Binding Title}" Foreground="{StaticResource TextFaintBrush}" FontSize="11" Margin="0,0,0,5" TextWrapping="Wrap" /><controls:ModuleAsyncImage ImageId="{Binding ImageCacheId}" ImageUrl="{Binding ImageUrl}" Width="500" Height="281" Margin="0" HorizontalAlignment="Left" PreviewClicked="ImagePreview_Click" /></StackPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                  </StackPanel>
                </Border>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
          <TextBlock x:Name="StatusText" Text="正在加载" Foreground="{StaticResource TextFaintBrush}" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
      </Grid>
    </Grid>
  </Grid>
</UserControl>
